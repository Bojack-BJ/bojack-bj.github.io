# Xiaotong Li Personal Website

This repository is a Jekyll source site for `bojack-bj.github.io`.

## Local preview

```sh
bundle install
bundle exec jekyll serve
```

The site builds automatically on GitHub Pages through `.github/workflows/pages.yml`.
